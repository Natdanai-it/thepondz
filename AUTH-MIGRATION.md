# แผนย้ายจากรหัสกลุ่มสู่บัญชีลูกค้ารายบุคคล

เว็บชุดนี้ยังเป็น Static GitHub Pages และใช้ไฟล์คอร์สที่เข้ารหัสด้วยรหัสกลุ่ม เหมาะกับลูกค้าจำนวนน้อย แต่ยังไม่ใช่ระบบ Login รายบุคคล

## สถาปัตยกรรมที่แนะนำ

ใช้ Supabase เป็น Backend ร่วมกับเว็บเดิม:

1. Supabase Auth จัดการบัญชีและการรีเซ็ตรหัสผ่าน
2. ตาราง `course_access` เชื่อม `user_id`, `course_id`, `expires_at`, `revoked_at`
3. Private Storage Bucket เก็บวิดีโอ สไลด์ และไฟล์ลูกค้า
4. Row Level Security ตรวจสิทธิ์ทุกคำขอจากผู้ใช้
5. Edge Function ออก Signed URL อายุสั้นเมื่อผู้ใช้มีสิทธิ์
6. ตาราง `access_log` เก็บเวลาเข้าใช้ คอร์ส และผลการอนุญาต

## ความสามารถหลังย้าย

- กำหนดวันหมดอายุและยกเลิกสิทธิ์รายคน
- แยกไฟล์ของลูกค้าแต่ละราย
- ดูประวัติการเข้าใช้โดยไม่เก็บรหัสผ่านของลูกค้าเอง
- เปลี่ยนคอร์สหรือเพิ่มไฟล์โดยไม่ต้องแจก ZIP และรหัสกลุ่มใหม่

## สิ่งที่ต้องเตรียมก่อนเริ่ม

- Supabase Project URL และ Public anon key
- รายชื่อคอร์สและกฎสิทธิ์ของลูกค้า
- โครงสร้างโฟลเดอร์ไฟล์ Private
- นโยบายเก็บ Log และระยะเวลาเก็บข้อมูล
- อีเมลผู้ส่งสำหรับ Reset password

ห้ามนำ Service Role key, รหัสผ่านลูกค้า หรือ Signed URL ถาวรขึ้น GitHub เด็ดขาด
